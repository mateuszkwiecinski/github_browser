plugins {
    id 'com.starter.config'
    id 'com.starter.versioning'
}

commonConfig {
    javaFilesAllowed false
    androidPlugin {
        compileSdkVersion 29
        minSdkVersion 26
        targetSdkVersion 29
    }
}
